<%= render @discussion %>

<h2>Comments</h2>
<%= render @discussion.comments %>



<% if (member_signed_in?)  %>
  <h2>Add a comment:</h2>
  <%= render 'comments/form' %>
<% end %>

<%= link_to 'All discussions', project_discussions_path %>